import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ng.facilitymanager.app",
  appName: "Facility Manager",
  webDir: "dist",
};

export default config;
