const USER = "user";
const TOKEN = "auth-token";
const REFRESH_TOKEN = "refresh-token";
const ESTATE = "estate";

const STORAGE_KEYS = {
  USER,
  TOKEN,
  REFRESH_TOKEN,
  ESTATE,
};

export default STORAGE_KEYS;
